# Group 9 to Group 10 transition

## Preserve the ongoing experiment

The original checkout is `/Users/doggonebastard/Antigravity/ML for CryoMN`. The implementation branch worktree is `/Users/doggonebastard/Antigravity/ML for CryoMN-group10`, branch `codex/v2-methodology-group10`.

Group 9's frozen proposal/worksheet and endpoint interpretation stay unchanged. The new endpoint does not change past maxima into terminal-force labels. All six provided raw traces were reviewed read-only; the actual ingestable worksheet has not been populated by this implementation.

## Before starting Group 10

1. Finish the actual Group 9 viability/intact/mechanical worksheet under its frozen contract. The latest inspection found these input columns blank in the live CSV.
2. Run the normal Group 9 update with `--skip-generate` if using the old main checkout. This prevents the old selector from freezing a Group 10 proposal before the reviewed code is in use. Do not initialize/rebuild the campaign database.
3. Bring the completed campaign evidence and archives into the reviewed execution environment through the normal branch transition. Keep one authoritative campaign database. Merely running Python from a different worktree uses that worktree's default data paths, which may be stale. The code change must be deployed/reviewed before expecting the old main command to use it.
4. Generate an isolated preview from the actual completed Group 9 database using explicit `--formulations`, `--observations`, `--output-dir` and `--total-candidate-pool` paths. Do not use the synthetic validation slate as the wet-lab recommendation.
5. Check the reference, primary roles, ordered backups and effective settings. Group 10 metadata must say `group10_workflow_v4`, proposal schema 4 and `terminal_force_08mm_after_1N_v1`. GP/noise revision flags remain false; endpoint revision is true.
6. Freeze the actual Group 10 proposal using the reviewed branch in the chosen campaign checkout. Run `check_group10_readiness.py --campaign-root <that-checkout> --require-ready`. Start only when the intended active worksheet and frozen proposal agree.

## During Group 10 and subsequent updates

Each compression specimen gets a CSV result row with the original Instron path and actual loaded-needle count when known. Replicate numbers are obtained from these rows; no advance count is configured. Include fresh paired viability/intact data for follow-up/control specimens as appropriate. Mark a lost attempt explicitly without inventing a force. Keep the compatibility force and old stiffness input columns blank for automatic extraction.

The first qualifying run of >=1 N lasting >=0.100 s with >=5 samples sets the origin. Extract force at +0.800 mm by adjacent-sample interpolation. There is no fracture/drop alternative. The test must reach that displacement; a short trace does not become a low-force exact result.

The round update verifies the frozen settings, ingests total and nominal force with provenance, reports attempted versus interpretable workloads, and generates the next proposal. The control is excluded from the production GP, no normalization occurs, and old-definition maxima cannot enter the new mechanical target.

## Before the first full-mechanics proposal

Review the existing model/noise/acquisition audit and explicitly choose any GP methodology change. Phase entry alone does not perform that switch. The endpoint decision has already been made for Group 10 and is independent of this later modeling decision. Mechanical phase gates count comparable paired evidence; retaining the old labels in the archive does not make them comparable to the new endpoint.
